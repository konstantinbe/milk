#!/usr/bin/env ruby

require 'rubygems'
require 'commander/import'
require 'find'
require 'FileUtils'

program :version, '0.0.01'
program :description, 'Build utility for the Milk framework'

command :build do |c|
  c.syntax = 'milk build [options]'
  c.summary = ''
  c.description = ''
  c.example 'description', 'command example'
  c.option '--some-switch', 'Some switch that does something'

  c.action do |args, options|
    # find all files
    files = []
    Find.find('source') do |path|
      files << path if File.extname(path) == '.coffee'
    end

    # determine dependencies
    dependencies = {}
    files.each do |file|
      dependencies[file] = [] # TODO: implement.
    end

    # build dependency graph

    # sort files using the dependency graph

    # if there is a cyclic dependency, abort

    # otherwise wrap the contents of each files by a closure

    # concatenate all files

    # prepend definition of require function

    # compile
  end
end
